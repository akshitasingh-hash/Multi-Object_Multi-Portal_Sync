<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cursor_Key__c</fullName>
    <caseSensitive>false</caseSensitive>
    <description
  >SObject_Name__c + '|' + Portal__c. Unique and an External Id so the batch can Database.upsert by this key with no pre-query -- which also makes a duplicate row impossible if two runs ever race.</description>
    <externalId>true</externalId>
    <label>Cursor Key</label>
    <length>120</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
