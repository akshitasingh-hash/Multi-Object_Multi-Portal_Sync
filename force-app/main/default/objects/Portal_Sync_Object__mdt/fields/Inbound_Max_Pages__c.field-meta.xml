<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Inbound_Max_Pages__c</fullName>
    <description
  >Maximum search pages per (object, portal) per run. Blank defaults to 20. Bounds callouts, heap and CPU inside one transaction; hitting the cap defers the rest to the next run and still advances the watermark, so forward progress is guaranteed.</description>
    <externalId>false</externalId>
    <fieldManageability>SubscriberControlled</fieldManageability>
    <label>Inbound Max Pages</label>
    <precision>4</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
